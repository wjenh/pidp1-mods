Test of the Type 340 point display quadrant addressing
// Draw a dot in the middle, upper left quadrant, lower left quadrant, upper right quadrant, lower right quadrant
#include <TYPE340/type340defs.ah>

#define CENTER 0d512
#define Xleft 0d256
#define Xright 0d768
#define YBOTTOM 0d256
#define YTOP 0d768

100/
top, lio [prog
    dla
    dss
    jmp .-1
    jmp top

// Begins in PARAM mode
prog, scale(0) intensity(3) next(point)
    y(CENTER) next(point)
    x(CENTER) show next(point)
    y(YTOP) next(point)
    x(Xleft) show next(point)
    y(YBOTTOM) next(point)
    x(Xleft) show next(point)
    y(YTOP) next(point)
    x(Xright) show next(point)
    y(YBOTTOM) next(point)
    x(Xright) show next(parameter)
    halt

start top
